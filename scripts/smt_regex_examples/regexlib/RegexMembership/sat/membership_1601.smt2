(explore-derivatives (re.union (re.++ (str.to_re "")(re.++ (re.range "A" "A") (str.to_re "")))(re.union (re.++ (str.to_re "")(re.++ (re.range "C" "C") (str.to_re "")))(re.union (re.++ (str.to_re "")(re.++ (re.range "D" "D") (str.to_re "")))(re.union (re.++ (str.to_re "")(re.++ (re.range "F" "F") (str.to_re "")))(re.union (re.++ (str.to_re "")(re.++ (re.range "H" "H") (str.to_re "")))(re.union (re.++ (str.to_re "")(re.++ (re.range "K" "K") (str.to_re "")))(re.union (re.++ (str.to_re "")(re.++ (re.range "L" "L") (str.to_re "")))(re.union (re.++ (str.to_re "")(re.++ (re.range "M" "M") (str.to_re "")))(re.union (re.++ (str.to_re "")(re.++ (re.range "O" "O") (str.to_re ""))) (re.++ (str.to_re "")(re.++ (re.range "P" "P") (str.to_re "")))))))))))))
