(explore-derivatives (re.union (re.++ (str.to_re "")(re.++ (str.to_re (seq.++ "*" (seq.++ "." "")))(re.++ (re.union (re.range "A" "Z") (re.range "a" "z"))(re.++ (re.union (re.range "A" "Z") (re.range "a" "z"))(re.++ (re.union (re.range "A" "Z") (re.range "a" "z")) (str.to_re "")))))) (re.++ (str.to_re "")(re.++ (str.to_re (seq.++ "*" (seq.++ "." (seq.++ "*" "")))) (str.to_re "")))))
