(explore-derivatives (re.++ (str.to_re "")(re.++ (re.opt (re.range "$" "$"))(re.++ (re.union ((_ re.loop 1 20) (re.range "0" "9"))(re.union (re.++ ((_ re.loop 1 2) (re.range "0" "9")) ((_ re.loop 0 6) (re.++ (re.opt (re.range "," ",")) ((_ re.loop 3 3) (re.range "0" "9"))))) (re.++ ((_ re.loop 3 3) (re.range "0" "9")) ((_ re.loop 0 5) (re.++ (re.opt (re.range "," ",")) ((_ re.loop 3 3) (re.range "0" "9"))))))) (str.to_re "")))))
