(explore-derivatives (re.++ (re.++ (str.to_re "")(re.++ (str.to_re (seq.++ "(" (seq.++ "0" "")))(re.++ ((_ re.loop 2 2) (re.range "0" "9")) (re.range ")" ")")))) (re.++ ((_ re.loop 6 7) (re.range "0" "9")) (str.to_re ""))))
