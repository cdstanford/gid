(explore-derivatives (re.++ (str.to_re "")(re.++ (re.opt (re.range "-" "-"))(re.++ ((_ re.loop 1 10) (re.range "0" "9"))(re.++ (re.opt (re.range "." "."))(re.++ (re.opt (re.++ (re.range "0" "9") (re.range "0" "9"))) (str.to_re "")))))))
