(explore-derivatives (re.union (re.++ (str.to_re "")(re.++ (re.opt (re.range "-" "-"))(re.++ ((_ re.loop 0 2) (re.range "0" "9"))(re.++ (re.opt (re.++ (re.range "." ".") ((_ re.loop 1 2) (re.range "0" "9")))) (str.to_re ""))))) (re.++ (str.to_re "")(re.++ (re.opt (re.range "-" "-"))(re.++ (str.to_re (seq.++ "1" (seq.++ "0" (seq.++ "0" ""))))(re.++ (re.opt (re.++ (re.range "." ".") ((_ re.loop 1 2) (re.range "0" "0")))) (str.to_re "")))))))
