(explore-derivatives (re.++ (str.to_re "")(re.++ (re.opt (re.range "$" "$"))(re.++ ((_ re.loop 0 2) (re.union (re.range "A" "Z") (re.range "a" "z")))(re.++ (re.opt (re.range "$" "$"))(re.++ ((_ re.loop 0 5) (re.range "0" "9"))(re.++ (re.opt (re.range ":" ":"))(re.++ (re.opt (re.range "$" "$"))(re.++ ((_ re.loop 0 2) (re.union (re.range "A" "Z") (re.range "a" "z")))(re.++ (re.opt (re.range "$" "$"))(re.++ ((_ re.loop 0 5) (re.range "0" "9")) (str.to_re ""))))))))))))
