(explore-derivatives (re.++ (str.to_re "")(re.++ (re.opt (re.range "0" "9"))(re.++ (re.* (re.union (re.range "," ",") (re.range "0" "9")))(re.++ (re.opt (re.range "." "."))(re.++ ((_ re.loop 1 2) (re.range "0" "9")) (str.to_re "")))))))
