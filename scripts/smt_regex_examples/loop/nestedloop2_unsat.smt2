(explore-derivatives (re.++ (re.++ (re.++ (re.++ (re.++ (re.++ (re.++ (re.++ (re.++ (re.++ (re.* (re.++ (str.to_re "a") (re.* (str.to_re "bc")))) (str.to_re "d")) (re.+ (str.to_re "bc"))) (re.* (re.++ (str.to_re "a") (re.* (str.to_re "bc"))))) (str.to_re "dabc")) (re.* (re.++ (str.to_re "a") (re.* (str.to_re "bc"))))) (str.to_re "d")) (re.+ (str.to_re "bc"))) (re.* (re.++ (str.to_re "a") (re.* (str.to_re "bc"))))) (str.to_re "d")) (re.inter (str.to_re "a") (str.to_re "b"))))
